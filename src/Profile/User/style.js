import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    container:{

    },
    image:{
        height:62,
        width:62,
        margin:20,
        borderColor: '#ffff',
        borderStyle: 'dotted',
        borderWidth: 5,
        borderRadius: 50,
    },
    user:{
        flexDirection:'row'
    }
});
export default styles;  