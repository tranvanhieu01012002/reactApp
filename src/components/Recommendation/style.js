import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    rec_head: {
        fontSize: 24,
        fontWeight: 'bold',
        textAlign: 'center',
        marginBottom: 10,
    },
    recommendation: {
        flexDirection: 'row',
        flexWrap: 'wrap',
        margin: 1,
    },
    image: {
        width: 117,
        height: 150,
    }
});
export default styles;