import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    image:{
        width:'100%',
        height:150,
        borderRadius:20,
        overflow: "hidden",
        marginBottom:10,
        marginTop:10
    }
});
export default styles;